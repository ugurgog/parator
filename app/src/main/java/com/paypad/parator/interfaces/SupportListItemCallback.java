package com.paypad.parator.interfaces;

import com.paypad.parator.enums.ItemsEnum;
import com.paypad.parator.enums.SupportListEnum;

public interface SupportListItemCallback {
    void OnItemReturn(SupportListEnum itemType);
}
